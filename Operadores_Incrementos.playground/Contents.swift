import UIKit

// INCREMENTOS, OPERADORES LOGICOS

let result = 2 <= 3

let x = 10
let y = 20
let res = x == y

let firstName = "Hugo"
let lastName = "Pinheiro"
firstName >= lastName

// Igual(==), Diferente(!=), Atribuição(=), Menor(<), Maior(>), Maior_Igual(>=), Menor_Igual(<=)

// Inversão(!), and(&&), or(||)

let isStudant = true
let isFederal = true

!isStudant
isStudant && 5 > 3
let r = isFederal || isStudant
print(r)

// Incremento ( + && -)
var z = 10
z += 2
var w = 8
w -= 5


